codecov:
  require_ci_to_pass: yes

coverage:
  status:
    project:
      default: false
      tests:
        paths:
          - "*_test.go"
        target: 80%
    patch:
      default:
        target: 80%
